<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 扫描 controller -->
    <context:component-scan base-package="com.controller"/>
    <aop:aspectj-autoproxy/>
    <!-- 或 静态资源放行 -->
    <mvc:default-servlet-handler/>

    <!-- 配置 日期 处理 -->
    <!-- 注册处理器映射器 添加 conversion-service  -->
    <mvc:annotation-driven conversion-service="myConversionService"/>

    <!-- 创建bean 类中实现 convert -->
    <bean id="dateConvertUtil" class="com.utils.DateConvertUtil">
        <property name="parsePatterns">
            <array>
                <value>yyyy-MM-dd</value>
                <value>yyyy-MM-dd HH:mm:ss</value>
                <value>yyyy/MM/dd</value>
                <value>yyyy/MM/dd HH:mm:ss</value>
            </array>
        </property>
    </bean>

    <!-- 创建FormattingConversionServiceFactoryBean bean  -->
    <bean id="myConversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
        <property name="converters">
            <set>
                <ref bean="dateConvertUtil"/>
            </set>
        </property>
    </bean>

    <!-- 自定义视图解析器  InternalResourceViewResolver-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!--&lt;!&ndash; 前缀 &ndash;&gt;
        <property name="prefix" value="/WEB-INF/jsp/"/>
        &lt;!&ndash; 后缀 &ndash;&gt;
        <property name="suffix" value=".jsp"/>-->
        <!-- 文件上传 -->
        <property name="viewClass" value="org.springframework.web.servlet.view.JstlView"/>
    </bean>


    <!-- 配置 拦截器 -->
    <!--<mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <bean class="com.forward.utils.HandlerInterceptorImplUtils"/>
        </mvc:interceptor>
    </mvc:interceptors>-->
</beans>